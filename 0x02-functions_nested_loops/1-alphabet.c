#include "main.h"
/**
*main - Program that prints the alphabet in lowercase
*
* Return: Always(Success)
*/
void print_alphabet(void);
{
char ch;
for (ch = 'a'; ch <= 'z'; ch++)
_putchar(ch);
_putchar ('\n');
return (0);
}
