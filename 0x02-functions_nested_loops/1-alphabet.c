#include "main.h"
/**
*main -prints the alphabet in lowercase
*
* Return: Always(Success)
*/
void print_alphabet(void);
char c = 'a';
for (c = 'a'; ch <= 'z'; ch++)
_putchar(c);
_putchar('\n');
return (0);
}
